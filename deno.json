{
  "tasks": {
    "dev": "deno run --watch main.ts",
    "docker:build": "docker build -t seb-monitor .",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "docker:logs": "docker compose logs -f",
    "test:docker": "deno test --allow-run --allow-read --allow-net tests/docker_test.ts"
  },
  "imports": {
    "@std/assert": "jsr:@std/assert@1",
    "fast-diff": "npm:fast-diff"
  },
  "version": "0.7.0"
}
